<UserControl
    x:Class="DBLauncher.Controls.InfoItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DBLauncher.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel Spacing="10" VerticalAlignment="Center" Orientation="Horizontal" HorizontalAlignment="Center">
        <FontIcon Glyph="{x:Bind InfoIcon}" FontSize="25" FontWeight="Light"/>
        <StackPanel>
            <TextBlock Text="{x:Bind Header}" FontWeight="Medium" FontFamily="Segoe UI" FontSize="15"/>
            <TextBlock Text="{x:Bind Info}" FontWeight="Light" FontFamily="Segoe UI" FontSize="13"/>
        </StackPanel>
    </StackPanel>
</UserControl>
